rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // STEP 1 안전 기본값: 기능별 규칙을 확정하기 전에는 모든 접근을 차단한다.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

